# chromedriver / phantomjs (e2e legacy) échouent sur Apple Silicon et Node récent.
# Le dev (webpack) et le build prod n'en ont pas besoin.
ignore-scripts=true
# Webpack 1 / Vue 1 : peer deps non résolues par npm 7+
legacy-peer-deps=true
